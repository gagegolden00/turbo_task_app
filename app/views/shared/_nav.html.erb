
<nav class="bg-gray-800 py-4 px-8">
  <div class="flex justify-between items-center">
    <div class="flex items-center space-x-4">
      <a href="/" class="text-white text-xl font-bold">HOME</a>
    </div>
    <div class="flex space-x-4">
      <% if user_signed_in? %>
        <%= link_to 'LOGOUT', destroy_user_session_url, data: { turbo_method: :delete }, class: "text-white hover:text-gray-300" %>
        <%= link_to 'VIEW MY TASKS', tasks_url, class: "text-white hover:text-gray-300" %>
      <% else %>
        <%= link_to 'SIGN UP', new_user_registration_path, data: {turbo_frame: 'modal'}, class: "text-white hover:text-gray-300" %>
        <%= link_to 'SIGN IN', new_user_session_path, data: {turbo_frame: 'modal'}, class: "text-white hover:text-gray-300" %>
      <% end %>
    </div>
  </div>
</nav>
